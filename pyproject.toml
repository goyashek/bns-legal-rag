[project]
name = "bns-legal-rag"
version = "0.1.0"
description = "RAG for Indian criminal law (BNS/BNSS/BSA) with hybrid retrieval, a deterministic citation validator, and dual evaluation."
readme = "README.md"
requires-python = ">=3.11,<3.14"
license = { text = "MIT" }

# core deps.
# resolve to a lockfile (`uv lock` / `uv sync`) before install so builds are reproducible.
dependencies = [
    # --- agent orchestration ---
    "langgraph>=0.2",
    "langchain-core>=0.3",
    "langchain-openai>=0.2",         # DeepSeek judge through its OpenAI-compatible API
    # --- ingestion / chunking ---
    "chonkie[qdrant,model2vec,st]>=0.4",  # SemanticChunker + native QdrantHandshake. model2vec = potion-base-8M backbone (locked stack; without it Chonkie silently falls back to a sentence-transformer). st = fallback embeddings.
    "pymupdf>=1.24",                 # gazette PDF parsing
    # --- retrieval ---
    "qdrant-client>=1.12",           # vector DB (native hybrid)
    "rank-bm25>=0.2.2",              # in-process sparse (~1k chunks)
    "sentence-transformers>=3.0",    # bge-large-en-v1.5 embeddings + bge-reranker-base
    # --- structured output ---
    "pydantic>=2.9",
    "instructor>=1.6",
    # --- eval ---
    "ragas>=0.2",                    # RAGAS 50-scenario diagnostics
    "datasets>=3.0",                 # load the BhashaBench slice from HuggingFace
    # --- api / frontend ---
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "streamlit>=1.40",
    # --- observability / config ---
    "langsmith>=0.1",                # LangGraph tracing
    "python-dotenv>=1.0",
]

[project.optional-dependencies]
dev = [
    "ruff>=0.6",
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]   # pycodestyle, pyflakes, isort, pyupgrade, bugbear

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
